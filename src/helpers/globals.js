/**
 * Store of all global parameters used in the application.
 * @module globals
 */

/**
 * Backend URL.
 * @type {string}
 */
export const DATA_URL = 'http://localhost:3000/';

/**
 * Backend endpoint <i>/user</i> URL.
 * @type {string}
 */
export const USER_URL = DATA_URL + 'user/';
